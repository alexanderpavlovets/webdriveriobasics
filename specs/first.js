describe('1', ()=>{
    it('1', ()=>{
        browser.url('')
        console.log('Sucess')
        console.log('are you really sync ?')
        console.log('if it is sync - this one should be the last message')
        expect(1).toBe(1)
    })
})
